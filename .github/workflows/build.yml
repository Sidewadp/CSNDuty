name: Build APK

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install dependencies
        run: npm install

      - name: Install Capacitor CLI
        run: npm install -g @capacitor/cli

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      # --- NAVIN ADD KELELYA STEPS ---

      - name: Build Web Assets
        run: npm run build  # Tumche HTML/JS files build karnyasathi

      - name: Capacitor Sync
        run: npx cap sync android  # Web code Android folder madhe copy karnyasathi

      - name: Build Android APK
        run: |
          cd android
          chmod +x gradlew
          ./gradlew assembleDebug  # Debug APK banavnyasathi (Test sathi sopa jato)

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: CSNDuty-App
          path: android/app/build/outputs/apk/debug/app-debug.apk
